<% @knowledges.each do |k| %>
  <div>
    ユーザー:<%= k.user.name %>
    <%= k.title %>
    <%= k.body.truncate(15) %>
    <%= k.category.category_name %>
    <%= k.created_at.strftime('%Y/%m/%d %H:%M') %>
  </div>
<% end %>